#VISUALISATION

#scatter PLot
# to get all the colors colours()
x <- 1:10
y <- 111:120
#12-13 different designs
plot(x,y, col = "violet",pch = 13, main = "Scatter PLot", xlab = "X-axis", ylab = "Y-axis")

d <- c(1,2,3,5,8,6,6,3,3,9,5,4)
barplot(d,col="skyblue")
#histogram shows the frequency of occurence of each data point
hist(d,col="skyblue")















